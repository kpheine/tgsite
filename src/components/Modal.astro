---
interface Props {
  id: string;
  labelledBy?: string;
  ariaLabel?: string;
  variant?: 'centered' | 'fullscreen' | 'media';
  className?: string;
  closeLabel?: string;
}

const {
  id,
  labelledBy,
  ariaLabel,
  variant = 'centered',
  className = '',
  closeLabel = 'Fechar popup',
} = Astro.props;
---

<dialog
  id={id}
  class:list={['modal', `modal--${variant}`, className]}
  aria-labelledby={labelledBy}
  aria-label={ariaLabel}
  data-modal
>
  <div class="modal__panel" data-modal-panel>
    <button class="modal__close" type="button" data-modal-close aria-label={closeLabel}>
      <span aria-hidden="true">×</span>
    </button>

    <slot />
  </div>
</dialog>
